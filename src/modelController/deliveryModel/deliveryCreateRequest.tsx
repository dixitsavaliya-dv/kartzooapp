export interface deliveryCreateRequest {
    firstname?: string,
    lastname?: string,
    email?: string,
    mobilenumber?: string,
    password?: string,
    selectedFile?: object,
    deliveryidproof?: object,
    deliveryaddressproof?: object,
}
