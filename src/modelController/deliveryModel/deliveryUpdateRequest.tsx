export interface deliveryUpdateRequest {
    id?:string,
    firstname?: string,
    lastname?: string,
    email?: string,
    mobilenumber?: string,
    password?: string,
    selectedFile?: object,
    deliveryidproof?: object,
    deliveryaddressproof?: object,
}
